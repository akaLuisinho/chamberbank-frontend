import styled from 'styled-components'

export const Container = styled.div`
    position: fixed;
    height: 100vh;
    right: 0;
    width: 15vw;
    max-width: 250px;
    min-width: 100px;
    background: var(--yellow);
`
